/*Immutable is a concept in which the value of a variable cannot be changed.

->The primitve values are immutable.
->once a primitive value is created, it cannot be changed.

Benefits of using immutable objects.

    To improve performance 
    To reduce memory use 
    Thread-safety 
    Lower developer mental burden 

eg:
    let a=10;
    let b=a;
    b=15;
    console.log(a,b);

*/